import React from 'react';
import { useAppContext } from '../context/AppContext';

const Header: React.FC = () => {
  const {  } = useAppContext();

  return (
    <header className="bg-white border-b border-gray-200 py-5 px-4 flex items-center justify-between shadow-sm">
   
    </header>
  );
};

export default Header;